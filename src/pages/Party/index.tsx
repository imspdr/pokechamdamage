import { FC } from 'react';
import styled from '@emotion/styled';

const Container = styled.div`
  width: 100%;
  display: flex;
  flex-direction: column;
`;

const PartyPage: FC = () => {
  return (
    <Container>
      <div>파티 구성 페이지 Placeholder</div>
    </Container>
  );
};

export default PartyPage;
